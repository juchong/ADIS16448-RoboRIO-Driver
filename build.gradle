allprojects {
    apply plugin: 'maven-publish'
    repositories {
		mavenCentral()
    }
}

subprojects{
	publishing {
		repositories {
			maven {
				println rootProject.path.toString() 
				url = rootProject.projectDir.toString() + "/maven"
			}
		}
	}
}